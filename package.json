{
  "name": "prophet",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev:backend": "cd backend && yarn start:dev",
    "dev:frontend": "cd frontend && yarn dev",
    "build:backend": "cd backend && yarn build",
    "build:frontend": "cd frontend && yarn build",
    "install:all": "yarn install && cd backend && yarn install && cd ../frontend && yarn install",
    "backup:db": "mkdir -p backend/backups && export $(grep -v '^#' .env | xargs) && /opt/homebrew/opt/postgresql@17/bin/pg_dump \"$DATABASE_URL\" -Fc -f backend/backups/prophet-$(date +%Y%m%d-%H%M).dump && echo '✓ Backup saved to backend/backups/'"
  }
}
